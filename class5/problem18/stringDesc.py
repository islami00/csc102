# """
# strings are handled by the computer by storing them in a memory block partitioned based on the characters
# the blocks are ordered as written like an array from 0-n
# indexes accessed with square brackets
# """

# name, store first name, print all alphabets in it.

name = "Islam"

for i in name:
    print(i)

print("there are", len(name), "letters in my name")
